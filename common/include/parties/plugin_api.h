#pragma once

// Compatibility include for internal targets that still get headers from
// common/include. The canonical plugin SDK header lives under sdk/include.
#include "../../../sdk/include/parties/plugin_api.h"
